// app/(admin)/admin/questions/layout.tsx
// PORTED FROM CERTISTRY-APP - FULL CODE
import { ReactNode } from "react";

export default function AdminQuestionsLayout({ children }: { children: ReactNode }) {
  return (
    <>
      {children}
    </>
  );
}
